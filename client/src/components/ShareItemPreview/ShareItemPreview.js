import styles from './styles';
import React from 'react';
import { withStyles } from '@material-ui/core';

const ShareItemPreview = ({ classes, tags }) => {
	return (
		<div>
			<h1>Preview here</h1>
		</div>
	);
};

export default withStyles(styles)(ShareItemPreview);
